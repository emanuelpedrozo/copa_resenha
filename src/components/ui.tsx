import Link from 'next/link';
export function Empty({icon='🎮',title,text}:{icon?:string;title:string;text:string}){return <div className="card py-12 text-center"><div className="mb-3 text-4xl">{icon}</div><h3 className="font-black">{title}</h3><p className="mt-1 text-sm text-muted">{text}</p></div>}
export function PageTitle({title,subtitle,action}:{title:string;subtitle?:string;action?:React.ReactNode}){return <div className="mb-6 flex items-end justify-between gap-4"><div><h1 className="text-2xl font-black">{title}</h1>{subtitle&&<p className="mt-1 text-sm text-muted">{subtitle}</p>}</div>{action}</div>}
export function MatchRow({match}:{match:any}){return <Link href={`/jogos/${match.id}`} className="grid grid-cols-[1fr_auto_1fr] items-center gap-3 border-b border-white/[.06] px-4 py-4 last:border-0"><div><b className="block">{match.homePlayer.nickname}</b><span className="text-[10px] text-muted">Casa</span></div><div className="text-center">{match.homeScore===null?<b className="text-pitch">VS</b>:<b className="rounded-lg bg-white/[.06] px-3 py-2 text-lg">{match.homeScore} × {match.awayScore}</b>}<span className="mt-2 block text-[9px] font-bold uppercase tracking-wider text-muted">{match.status==='WAITING_CONFIRMATION'?'A confirmar':match.status==='CONFIRMED'?'Finalizado':match.status==='DISPUTED'?'Contestado':'Pendente'}</span></div><div className="text-right"><b className="block">{match.awayPlayer.nickname}</b><span className="text-[10px] text-muted">Visitante</span></div></Link>}
