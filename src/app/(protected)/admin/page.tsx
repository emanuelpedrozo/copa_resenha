import Link from 'next/link';import {requireAdmin} from '@/lib/auth';import {activeChampionship} from '@/lib/data';import {resolveDispute} from '@/app/actions';import {Empty,PageTitle} from '@/components/ui';
export default async function Admin({searchParams}:{searchParams:{ok?:string}}){await requireAdmin();const c=await activeChampionship();if(!c)return <><PageTitle title="Administração" action={<Link href="/admin/campeonatos/novo" className="rounded-xl bg-pitch px-4 py-2 text-xs font-black text-ink">CRIAR CAMPEONATO</Link>}/><Empty title="Nenhum campeonato ativo" text="Crie a primeira competição."/></>;const waiting=c.matches.filter(m=>m.status==='WAITING_CONFIRMATION').length,disputed=c.matches.filter(m=>m.status==='DISPUTED');return <><PageTitle title="Administração" subtitle={c.name} action={<div className="flex gap-2"><Link href="/admin/usuarios" className="rounded-xl border border-white/10 px-3 py-2 text-xs font-bold">USUÁRIOS</Link><Link href="/admin/campeonatos/novo" className="rounded-xl bg-pitch px-3 py-2 text-xs font-black text-ink">NOVA COPA</Link></div>}/>{searchParams.ok&&<p className="mb-4 rounded-xl bg-pitch/10 p-3 text-pitch">Pendência resolvida.</p>}<div className="mb-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{[[c.participants.length,'Participantes'],[c.matches.length,'Partidas'],[waiting,'A confirmar'],[disputed.length,'Contestações']].map(([n,l])=><div className="card p-4" key={l}><b className="text-2xl text-pitch">{n}</b><span className="block text-xs text-muted">{l}</span></div>)}</div><h2 className="mb-3 font-black">Contestações abertas</h2>{disputed.length?<div className="space-y-4">{disputed.map(m=>{const d=m.disputes.find(x=>x.status==='OPEN');return <form action={resolveDispute} key={m.id} className="card p-5"><input type="hidden" name="matchId" value={m.id}/><div className="mb-4 flex justify-between gap-3"><div><b>{m.homePlayer.nickname} × {m.awayPlayer.nickname}</b><p className="mt-1 text-sm text-rose-300">{d?.reason}</p></div><Link href={`/jogos/${m.id}`} className="text-xs text-pitch">Detalhes</Link></div><div className="grid grid-cols-2 gap-3"><input name="homeScore" type="number" min="0" defaultValue={m.homeScore??0} required className="rounded-xl border border-white/10 bg-black/20 p-3"/><input name="awayScore" type="number" min="0" defaultValue={m.awayScore??0} required className="rounded-xl border border-white/10 bg-black/20 p-3"/></div><input name="reason" placeholder="Motivo da decisão administrativa" required minLength={3} className="mt-3 w-full rounded-xl border border-white/10 bg-black/20 p-3"/><button className="mt-3 rounded-xl bg-pitch px-5 py-3 font-black text-ink">CORRIGIR E CONFIRMAR</button></form>})}</div>:<Empty icon="✅" title="Nenhuma contestação" text="A resenha está em paz."/>}</>}
