import Link from 'next/link';
import {Gamepad2,Target,Users,ChevronRight} from 'lucide-react';
import {requireUser} from '@/lib/auth';
import {activeChampionship,championshipTable,formatLabel} from '@/lib/data';
import {Empty,MatchRow} from '@/components/ui';
export default async function Dashboard(){const user=await requireUser();const c=await activeChampionship();if(!c)return <Empty icon="🏆" title="Nenhum campeonato ativo" text="A resenha começa quando o administrador criar uma competição."/>;const table=championshipTable(c);const played=c.matches.filter(m=>m.status==='CONFIRMED').length;const next=c.matches.find(m=>(m.homePlayerId===user.id||m.awayPlayerId===user.id)&&m.status==='PENDING');const recent=[...c.matches].filter(m=>m.status==='CONFIRMED').reverse().slice(0,3);return <><section className="card relative mb-6 overflow-hidden p-5 shadow-glow"><span className="tag text-pitch">{c.status==='ACTIVE'?'Em andamento':c.status}</span><h1 className="mt-3 text-2xl font-black">{c.name}</h1><p className="mt-1 text-sm text-muted">{formatLabel[c.competitionFormat]} · {formatLabel[c.matchFormat]}</p><div className="mt-6 grid grid-cols-3 gap-3 border-t border-white/10 pt-4">{[[Users,c.participants.length,'jogadores'],[Gamepad2,`${played}/${c.matches.length}`,'jogos'],[Target,c.rounds.length,'rodadas']].map(([Icon,n,label]:any)=><div key={label} className="flex gap-2"><Icon size={16} className="mt-1 text-pitch"/><div><b className="block">{n}</b><span className="text-[10px] text-muted">{label}</span></div></div>)}</div></section>{next&&<><div className="mb-3 flex justify-between"><h2 className="tag text-muted">⚔ Próximo desafio</h2><span className="tag text-amber-400">{next.round?.name}</span></div><Link href={`/jogos/${next.id}`} className="card mb-7 block overflow-hidden"><div className="grid grid-cols-[1fr_auto_1fr] items-center p-6 text-center"><Player name={next.homePlayer.nickname}/><b className="text-xl text-pitch">VS</b><Player name={next.awayPlayer.nickname}/></div><div className="flex items-center justify-center gap-2 border-t border-white/10 bg-pitch/10 py-3 text-xs font-black text-pitch">VER PARTIDA <ChevronRight size={15}/></div></Link></>}<div className="mb-3 flex justify-between"><h2 className="font-black">Classificação</h2><Link href="/classificacao" className="text-xs font-bold text-pitch">Ver tabela</Link></div><section className="card mb-7 overflow-hidden"><div className="grid grid-cols-[35px_1fr_35px_45px_45px] border-b border-white/10 px-4 py-3 text-[9px] font-bold text-muted"><span>POS</span><span>JOGADOR</span><span>J</span><span>SG</span><span>PTS</span></div>{table.slice(0,5).map(r=><div key={r.id} className="grid grid-cols-[35px_1fr_35px_45px_45px] border-b border-white/[.06] px-4 py-3 text-xs last:border-0"><b>{r.position===1?'🥇':r.position}</b><b>{r.user.nickname}</b><span>{r.played}</span><span>{r.goalDifference>0?'+':''}{r.goalDifference}</span><b>{r.points}</b></div>)}</section><div className="mb-3 flex justify-between"><h2 className="font-black">Últimos resultados</h2><Link href="/jogos?status=CONFIRMED" className="text-xs font-bold text-pitch">Ver todos</Link></div><section className="card">{recent.length?recent.map(m=><MatchRow key={m.id} match={m}/>):<Empty title="Nenhum placar registrado ainda" text="Os resultados aparecerão aqui."/>}</section></>}
function Player({name}:{name:string}){return <div><div className="mx-auto mb-2 grid h-16 w-16 place-items-center rounded-full border-2 border-pitch bg-pitch/10 text-lg font-black text-pitch">{name.slice(0,2).toUpperCase()}</div><b>{name}</b></div>}
