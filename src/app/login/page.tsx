'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import {Gamepad2,Trophy} from 'lucide-react';
export default function Login(){const router=useRouter();const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({login:form.get('login'),password:form.get('password')})});const data=await res.json();if(!res.ok){setError(data.error);setLoading(false);return}router.replace('/dashboard');router.refresh()}return <main className="grid min-h-screen place-items-center px-4"><div className="w-full max-w-sm"><div className="mb-8 text-center"><div className="mx-auto mb-4 grid h-16 w-16 place-items-center rounded-2xl bg-pitch text-ink shadow-glow"><Trophy size={34}/></div><h1 className="text-3xl font-black">COPA <span className="text-pitch">RESENHA</span></h1><p className="mt-2 text-xs uppercase tracking-[.2em] text-muted">Futebol virtual. Resenha real.</p></div><form onSubmit={submit} className="card space-y-4 p-6"><div><label className="mb-2 block text-xs font-bold text-muted">USUÁRIO OU E-MAIL</label><input name="login" required autoComplete="username" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-pitch"/></div><div><label className="mb-2 block text-xs font-bold text-muted">SENHA</label><input name="password" type="password" required minLength={8} autoComplete="current-password" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-pitch"/></div>{error&&<p className="rounded-lg bg-rose-500/10 p-3 text-sm text-rose-300">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-pitch py-3 font-black text-ink disabled:opacity-60"><Gamepad2 size={19}/>{loading?'ENTRANDO...':'ENTRAR'}</button></form></div></main>}
